// kernel: References (Ref), keyed randomness and its stream registry, deterministic math (dmath), time, the scheduler, state stores, the event log, cause references and decision records, hashing and serialization.
// May import: nothing. See README.md and docs/architecture/universe-architecture.md.
export {};
