// app: Boot, device tier, settings, save and load screens, input routing; wires the host (worker or in-thread) to render and ui.
// May import: kernel, bridge, view, render, ui, host. See README.md and docs/architecture/universe-architecture.md.
export {};
