// causal: The Causal Field above the simulation: the observer ledger and its claims, resolvers (collapse, deepen, later), the macro history, history retention and summaries, and the explainer (why).
// May import: kernel, rules, gen, sim. See README.md and docs/architecture/universe-architecture.md.
export {};
